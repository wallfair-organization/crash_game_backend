apiVersion: apps/v1
kind: Deployment
metadata:
  name: crash-game
spec:
  template:
    spec:
      containers:
        - name: crash-game
          envFrom:
          - configMapRef:
              name: app-secrets
          - configMapRef:
              name: backend-production-values

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pump-dump-game
spec:
  template:
    spec:
      containers:
        - name: pump-dump-game
          envFrom:
          - configMapRef:
              name: app-secrets
          - configMapRef:
              name: backend-production-values
