apiVersion: apps/v1
kind: Deployment
metadata:
  name: crash-game
spec:
  template:
    spec:
      containers:
        - name: crash-game-backend
          envFrom:
          - configMapRef:
              name: app-secrets
          - configMapRef:
              name: crash-game-prod-values

